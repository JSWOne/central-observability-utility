<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.jswone.observability</groupId>
    <artifactId>central-observability-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>central-observability-parent</name>
    <description>Shared observability starters for JSW services: OTEL/MDC trace correlation, async-context propagation, PII-masking log converters, a generic request/response logging aspect, and structured business-error events</description>

    <!--
      Deliberately NOT inheriting from spring-boot-starter-parent: this build produces one
      artifact for Spring Boot 3.x consumers and another for Spring Boot 2.6+ consumers, and a
      single Boot parent cannot manage both lines. Each starter module imports its own
      spring-boot-dependencies BOM instead.
    -->

    <modules>
        <module>central-observability-core</module>
        <module>central-observability-utility</module>
        <module>central-observability-spring-boot2</module>
    </modules>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!--
          The core and Boot 2 modules compile against the OLDEST supported dependency versions
          and target Java 11, so their bytecode runs unchanged on both lines. Compiling against
          the newest and running on the oldest would be the unsafe direction.
        -->
        <java.legacy.version>11</java.legacy.version>
        <maven.compiler.release>21</maven.compiler.release>
        <spring-boot2.version>2.6.15</spring-boot2.version>
        <spring-boot3.version>3.4.2</spring-boot3.version>
        <spring2.version>5.3.27</spring2.version>
        <micrometer-observation.version>1.10.13</micrometer-observation.version>
        <slf4j.legacy.version>1.7.36</slf4j.legacy.version>
        <logback.legacy.version>1.2.11</logback.legacy.version>
        <log4j.legacy.version>2.17.2</log4j.legacy.version>
        <aspectjweaver.legacy.version>1.9.7</aspectjweaver.legacy.version>
        <lombok.version>1.18.36</lombok.version>
        <opentelemetry.version>1.45.0</opentelemetry.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>io.opentelemetry</groupId>
                <artifactId>opentelemetry-api</artifactId>
                <version>${opentelemetry.version}</version>
            </dependency>
            <dependency>
                <groupId>io.opentelemetry</groupId>
                <artifactId>opentelemetry-sdk-testing</artifactId>
                <version>${opentelemetry.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <extensions>
            <extension>
                <groupId>com.google.cloud.artifactregistry</groupId>
                <artifactId>artifactregistry-maven-wagon</artifactId>
                <version>2.2.0</version>
            </extension>
        </extensions>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <release>${maven.compiler.release}</release>
                        <!--
                          Annotation processors are discovered from the compile classpath on
                          purpose. Pinning annotationProcessorPaths would silence not just
                          Lombok but Log4j2's PluginProcessor, and without it the
                          Log4j2Plugins.dat descriptor that makes <PiiMaskingRewritePolicy/>
                          resolvable in a log4j2.xml is never generated.
                        -->
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.2.5</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <repositories>
        <repository>
            <id>central</id>
            <name>Maven Central Repository</name>
            <url>https://repo.maven.apache.org/maven2</url>
        </repository>
        <repository>
            <id>artifact-registry-snapshot</id>
            <name>JSW Artifact Registry (Snapshot)</name>
            <url>artifactregistry://asia-south1-maven.pkg.dev/modular-bucksaw-305821/jsw-one-msme-artifacts-snapshot</url>
            <releases>
                <enabled>false</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>artifact-registry</id>
            <name>JSW Artifact Registry (Release)</name>
            <url>artifactregistry://asia-south1-maven.pkg.dev/modular-bucksaw-305821/jsw-one-msme-artifacts</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
    </repositories>

    <distributionManagement>
        <repository>
            <id>artifact-registry</id>
            <url>artifactregistry://asia-south1-maven.pkg.dev/modular-bucksaw-305821/jsw-one-msme-artifacts</url>
        </repository>
        <snapshotRepository>
            <id>artifact-registry-snapshot</id>
            <url>artifactregistry://asia-south1-maven.pkg.dev/modular-bucksaw-305821/jsw-one-msme-artifacts-snapshot</url>
        </snapshotRepository>
    </distributionManagement>
</project>
